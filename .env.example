# Firebase Client SDK Configuration
# These keys are safe to be public and are required for the frontend app to connect to your Firebase project.
# You can find these in your Firebase project settings under "General".
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-auth-domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-storage-bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Firebase Cloud Messaging (FCM) VAPID Key
# This is required for sending push notifications to web clients.
# You can generate this key in your Firebase project settings under "Cloud Messaging" -> "Web configuration".
NEXT_PUBLIC_FIREBASE_VAPID_KEY=your-fcm-vapid-key

# Google Maps API Key
# Required for displaying static maps on the dashboard.
# Ensure this key is properly restricted to your domain to prevent unauthorized use.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Sentry Configuration
# This DSN is used to connect the application to Sentry for error reporting.
NEXT_PUBLIC_SENTRY_DSN="your-sentry-dsn-goes-here"

# --- Server-Side Environment Variables ---
# These are used by the Firebase Admin SDK on the server (e.g., in Next.js server components or API routes).
# These MUST be kept secret and should NOT be prefixed with NEXT_PUBLIC_.
# For Vercel, set these in the project's environment variables settings.

# Firebase Admin SDK Service Account Credentials
# These are required for the server-side auth guard.
# You can generate a service account key in your Firebase project settings under "Service accounts".
FIREBASE_CLIENT_EMAIL=your-firebase-client-email
FIREBASE_PRIVATE_KEY=your-firebase-private-key

# SendGrid API Key
# This is required for the `notifySOS` Cloud Function to send email fallbacks.
# This should be set as a secret in the Cloud Functions environment.
# Use `firebase functions:secrets:set SENDGRID_API_KEY`
SENDGRID_API_KEY=your-sendgrid-api-key